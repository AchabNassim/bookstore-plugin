# bookstore-plugin
WordPress plugin that allows the management of a bookstore. The plugin handles categories, subcategories, and CRUD (Create, Read, Update, Delete) operations for books, including image uploads. A shortcode is added to display the books in wordpress pages.
